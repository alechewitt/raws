=== Session 11: Global Service Signing + UTC Timestamps (2026-04-11) ===

ACCOMPLISHED:
- Fixed clippy warnings: map_or → is_some_and in driver.rs, complex return type alias in query.rs
- Implemented s3 website subcommand: Full PutBucketWebsite API integration with XML building,
  arg parsing (--index-document, --error-document), XML escaping. 17 unit tests.
- Implemented ecs deploy / cloudformation package as "not implemented" custom commands
- Fixed global service signing region: Added resolve_signing_region() using credentialScope from
  endpoints.json. Applied to both execute() and handle_wait_command(). 8 unit tests.
- Fixed UTC timestamps: Changed from chrono::Local to chrono::Utc for AWS CLI v2 parity.

KEY FILES CHANGED:
- src/cli/driver.rs: Clippy fix, ECS/CFN custom commands, signing region resolution
- src/cli/customizations/s3/website.rs: NEW - Full s3 website implementation
- src/core/endpoint/resolver.rs: resolve_signing_region() function (8 tests)
- src/core/protocol/mod.rs: UTC timestamp formatting

TEST RESULTS: 1175 tests passing
ISSUES STATUS: 48/48 passing (100%)

=== Session 12: Parity Testing + Multiple Fixes (2026-04-11) ===

ACCOMPLISHED:
- Fixed S3 Express endpoint routing: Only route to s3express-control for directory buckets
  (--x-s3 suffix) or bucket-less ops. Extracted into resolve_s3_endpoint_style() helper.
- Fixed paginator limit_key stripping: Route53 list-hosted-zones no longer shows MaxItems
  in auto-paginated output. Fixed both single-page and multi-page paths.
- Fixed JMESPath [*] list wildcard: Was parsed as multi-select, now a proper ListWildcard
  token that projects without flattening. Buckets[*].Name works correctly.
- Fixed text output: Empty results no longer print blank line. Top-level scalar arrays
  from --query now tab-separated on one line (not one per line).
- Fixed YAML excessive quoting: ARNs, instance IDs, timestamps, regions no longer
  unnecessarily quoted. Only quote colons followed by space, leading dash-space, etc.
- Extensive parity testing across 20+ services: STS, IAM, EC2, S3, DynamoDB, Lambda,
  KMS, SNS, SQS, Route53, CloudFront, CloudWatch, CloudFormation, Logs, Secrets Manager,
  SSM, ECR, ECS, AutoScaling. All match AWS CLI output (except known key ordering).

KEY FILES CHANGED:
- src/cli/driver.rs: S3 Express fix, empty output guard
- src/cli/jmespath.rs: ListWildcard token, [*] tokenizer fix
- src/cli/formatter/text.rs: Top-level scalar array tab formatting
- src/cli/formatter/yaml.rs: Relaxed quoting rules
- src/core/paginate.rs: limit_key stripping in merge_pages()

TEST RESULTS: 1180 tests passing (up from 1175), cargo clippy clean
ISSUES STATUS: 54/54 passing (100%)
  issues-06: 8/8 (5 new issues found and fixed via parity testing)

KNOWN REMAINING GAPS (low priority):
- JSON key ordering: serde_json sorts alphabetically vs AWS CLI response order
- s3api list-buckets: Prefix: null stripped by strip_nulls() (AWS CLI keeps it)
- YAML 'off' quoting: raws quotes 'off' (correct YAML 1.1), AWS CLI doesn't
