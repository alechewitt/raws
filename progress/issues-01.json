[
  {
    "id": "credential-chain-missing-providers",
    "description": "Credential chain only includes EnvCredentialProvider and ProfileCredentialProvider. The CredentialProcessProvider, AssumeRoleProvider, SsoCredentialProvider, and WebIdentityTokenProvider are all implemented but never wired into the chain. This means --profile fails for any profile using credential_process, SSO, assume_role, or web_identity.",
    "severity": "critical",
    "verify": [
      { "run": "env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN cargo run -- sts get-caller-identity --region us-west-2 --profile=alechewt-dev", "expect": "Should succeed using credential_process (ada) from the profile config, not fail with 'No credentials found'" },
      { "run": "env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN cargo run -- s3 ls --region us-west-2 --profile=alechewt-dev", "expect": "Should list buckets using profile credentials" }
    ],
    "passes": true,
    "affected_files": [
      "src/core/credentials/chain.rs"
    ],
    "notes": [
      "Fixed: build_credential_chain() in chain.rs now includes all 7 providers: Env, CredentialProcess, Profile, AssumeRole, SSO, WebIdentity, IMDS",
      "All 5 callsites replaced with shared function"
    ]
  },
  {
    "id": "profile-env-var-precedence",
    "description": "When --profile is specified, raws still uses environment variable credentials (AWS_ACCESS_KEY_ID etc.) instead of the profile's credential mechanism. AWS CLI uses the profile's credentials when --profile is explicitly provided. This causes raws to return data from the wrong AWS account.",
    "severity": "critical",
    "verify": [
      { "run": "AWS_ACCESS_KEY_ID=AKIAEXAMPLE AWS_SECRET_ACCESS_KEY=secret cargo run -- sts get-caller-identity --region us-west-2 --profile=alechewt-dev 2>&1", "expect": "Should use alechewt-dev profile credentials (credential_process), NOT the env var credentials" }
    ],
    "passes": true,
    "affected_files": [
      "src/core/credentials/chain.rs"
    ],
    "notes": [
      "Fixed: build_credential_chain(explicit_profile=true) skips EnvCredentialProvider",
      "args.profile.is_some() determines explicit_profile flag"
    ]
  },
  {
    "id": "output-default-overrides-config",
    "description": "The --output flag has default_value='json' in clap args, which means ConfigProvider always receives Some('json') and never checks the config file or AWS_DEFAULT_OUTPUT env var for the output format. Users who set output=text in their ~/.aws/config profile will always get JSON output.",
    "severity": "medium",
    "verify": [
      { "run": "AWS_DEFAULT_OUTPUT=text cargo run -- sts get-caller-identity --region us-east-1 2>&1", "expect": "Should output in text format, not JSON" }
    ],
    "passes": true,
    "affected_files": [
      "src/cli/args.rs",
      "src/cli/driver.rs",
      "src/core/config/provider.rs"
    ],
    "notes": [
      "Fixed: args.output changed from String with default_value='json' to Option<String>",
      "ConfigProvider::resolve_output now properly checks: --output flag > AWS_DEFAULT_OUTPUT > config file > 'json' fallback"
    ]
  },
  {
    "id": "credential-chain-not-shared",
    "description": "The credential chain construction is duplicated in 5 places across the codebase (driver.rs x2, s3/mod.rs, logs/mod.rs, cloudformation/mod.rs). Each copy must be kept in sync. Should be extracted to a shared build_credential_chain() function.",
    "severity": "medium",
    "verify": [
      { "run": "grep -rn 'ChainCredentialProvider::new' src/", "expect": "Should only appear once in a shared helper, not 5 separate locations" }
    ],
    "passes": true,
    "affected_files": [
      "src/core/credentials/chain.rs"
    ],
    "notes": [
      "Fixed: build_credential_chain() function in chain.rs, used by all 5 callsites",
      "ChainCredentialProvider::new now only appears in chain.rs (the shared builder + tests)"
    ]
  },
  {
    "id": "nonexistent-profile-error-message",
    "description": "When a nonexistent profile is used, raws says 'No credentials found' instead of telling the user the profile doesn't exist. AWS CLI says 'The config profile (X) could not be found' and exits immediately without trying other credential sources.",
    "severity": "low",
    "verify": [
      { "run": "env -u AWS_ACCESS_KEY_ID -u AWS_SECRET_ACCESS_KEY -u AWS_SESSION_TOKEN cargo run -- sts get-caller-identity --region us-east-1 --profile nonexistent-profile-xyz 2>&1", "expect": "Should say 'The config profile (nonexistent-profile-xyz) could not be found' not 'No credentials found'" }
    ],
    "passes": true,
    "affected_files": [
      "src/core/config/provider.rs",
      "src/cli/driver.rs"
    ],
    "notes": [
      "Fixed: ConfigProvider::validate_profile_exists() checks config and credentials files",
      "Called from driver.rs and all customization modules when args.profile.is_some()",
      "Error message matches AWS CLI: 'The config profile (X) could not be found'"
    ]
  },
  {
    "id": "imds-credential-provider-stub",
    "description": "The IMDS (EC2 Instance Metadata Service) credential provider is a stub file with no implementation. raws cannot resolve credentials on EC2 instances, ECS tasks, or Lambda functions that rely on instance/task role credentials.",
    "severity": "low",
    "verify": [
      { "run": "cat src/core/credentials/imds.rs", "expect": "Should contain a working IMDSv2 credential provider implementation, not a stub" }
    ],
    "passes": true,
    "affected_files": [
      "src/core/credentials/imds.rs",
      "src/core/credentials/chain.rs"
    ],
    "notes": [
      "Fixed: Full IMDSv2 implementation with ECS container credentials support",
      "ECS: checks AWS_CONTAINER_CREDENTIALS_RELATIVE_URI, hits http://169.254.170.2",
      "EC2: IMDSv2 PUT for token, GET for role name, GET for credentials",
      "1-second timeout, added as last provider in credential chain"
    ]
  }
]
